import React from "react";
import styles from "./Button_seek.module.css";

const Button_seek = () => (
  <div>
    <button className={styles["btn"]}>submit</button>
  </div>
);

export default Button_seek;
