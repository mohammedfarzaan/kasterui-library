import React from "react";
import styles from "./Button_bake.module.css";

const Button_bake = () => (
  <div>
    <div className={styles["content"]}>
      <div className={styles["text"]}>LOADING...</div>
      <div className={styles["text"]}>LOADING...</div>
    </div>
  </div>
);

export default Button_bake;
