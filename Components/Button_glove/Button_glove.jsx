import React from "react";
import styles from "./Button_glove.module.css";

const Button_glove = () => (
  <div>
    <button className={styles["btn"]}>
      <span>Modern Button</span>
    </button>
  </div>
);

export default Button_glove;
