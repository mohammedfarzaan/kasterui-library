import React from "react";
import styles from "./Button_grip.module.css";

const Button_grip = () => (
  <div>
    <button className={styles["button"] + styles["type1"]}></button>
  </div>
);

export default Button_grip;
