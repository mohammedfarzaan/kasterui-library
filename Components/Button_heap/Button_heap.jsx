import React from "react";
import styles from "./Button_heap.module.css";

const Button_heap = () => (
  <div>
    <button className={styles["box"]}>
      <p className={styles["text-button"]}>Button</p>
    </button>
  </div>
);

export default Button_heap;
