import React from "react";
import styles from "./Button_fate.module.css";

const Button_fate = () => (
  <div>
    <button className={styles["button"]}>Button</button>
  </div>
);

export default Button_fate;
