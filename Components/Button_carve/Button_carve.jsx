import React from "react";
import styles from "./Button_carve.module.css";

const Button_carve = () => (
  <div>
    <button className={styles["button"]}>Enter</button>
  </div>
);

export default Button_carve;
