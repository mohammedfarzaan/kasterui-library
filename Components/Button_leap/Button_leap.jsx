import React from "react";
import styles from "./Button_leap.module.css";

const Button_leap = () => (
  <div>
    <button className={styles["button"]}>Button</button>
  </div>
);

export default Button_leap;
